# https://hub.docker.com/_/python/
image: python:3.5

stages:
  - build
  - test

build:
  stage: build
  script: 
    - pip install -r requirements.txt

test:
  stage: test
  script:
    - pip install nose
    - nosetests -x